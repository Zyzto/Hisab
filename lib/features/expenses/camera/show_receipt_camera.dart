// Inline receipt camera (live preview via `camera`, or debug mock feed).
// Web without mock: [showReceiptCamera] still opens; real camera may be
// unavailable — callers use mock toggle on desktop/web, else image_picker.
export 'show_receipt_camera_io.dart';
