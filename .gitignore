# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IDEs
*.iml
*.ipr
*.iws
.idea/
#.vscode/  # Uncomment to ignore; launch configs are often shared
.cursor/

# Pub dependency overrides (local dev paths)
pubspec_overrides.yaml

# Flutter / Dart
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
lcov.info

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android (build artifacts; android/.gitignore has local.properties, key.properties)
android/secrets.properties
android/app/google-services.json
/android/app/debug
/android/app/profile
/android/app/release

# iOS (Firebase config)
ios/Runner/GoogleService-Info.plist

# Firebase Hosting cache
.firebase/

# Secrets (copy from *_example files)
lib/core/constants/app_secrets.dart
dart_defines_online.json

# Environment
.env
.env.*
!.env.example

# Node (if any tooling is used)
node_modules/

# Private / personal notes (not for other developers or users)
docs/_private/
