# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
/supabase/.temp/
/supabase/.branches/
/supabase/.migrations/
/supabase/.migrations/**

# IDEs / local agent state
*.iml
*.ipr
*.iws
.idea/
#.vscode/  # Uncomment to ignore; launch configs are often shared
# Local Cursor state — keep skills + hooks versioned for the team
.cursor/*
!.cursor/skills/
!.cursor/skills/**
!.cursor/hooks.json
!.cursor/hooks/
!.cursor/hooks/**

# Pub dependency overrides (local dev paths)
pubspec_overrides.yaml

# Flutter / Dart
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.dart_tool/analyzer/
.dart_tool/lint/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/
lcov.info

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android (build artifacts; android/.gitignore has local.properties, key.properties)
android/build/
android/secrets.properties
android/app/google-services.json
/android/app/debug
/android/app/profile
/android/app/release
**/*.keystore
**/*.jks

# iOS Firebase config
ios/Runner/GoogleService-Info.plist
**/GoogleService-Info.plist

# Firebase Hosting cache
.firebase/

# ── Secrets / local defines (never commit) ──────────────────────────────────
dart_defines_online.json
dart_defines_local.json

# Local secrets folder (keep README + .gitkeep only)
secrets/**
!secrets/.gitkeep
!secrets/README.md
**/fcm-service-account*.json
**/serviceAccount*.json
**/*-firebase-adminsdk-*.json
**/service-account*.json
**/credentials*.json
**/client_secret*.json
**/signing_keys.json
firebase-debug.log
firebase-debug.*.log
*.pem
*.p12
*.key

# Environment files (keep *.example)
.env
.env.*
!**/.env.example
!.env.example
supabase/.env
functions/.env

# Python (uv / venv)
.venv/

# Node (if any tooling is used)
node_modules/
functions/lib/

# Private / personal notes (not for other developers or users)
docs/_private/

# Plan docs (local; not committed)
plans/

# Supabase database backups (never commit)
supabase_backups/

# Local scratch (OCR photos/outputs, tool previews) — never commit
tmp/

# Test runner logs
logs/
