# Copy to .env and set values. Required for inviteRedirectPage (OG meta + redirect).
# Also set in CI when deploying Cloud Functions.
SUPABASE_URL=https://your-project.supabase.co
SITE_URL=https://hisab.shenepoy.com
